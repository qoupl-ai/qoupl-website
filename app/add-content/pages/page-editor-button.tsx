/**
 * Page Editor Button - DEPRECATED
 * 
 * Page creation is disabled. Pages are predefined and cannot be created from CMS.
 * This component is kept for backward compatibility but will not render anything.
 */

'use client'

// This component is no longer used - pages cannot be created from CMS
// Pages are predefined and can only be edited, not created or deleted
export default function PageEditorButton() {
  return null
}

